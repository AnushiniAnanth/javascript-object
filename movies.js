const movie ={
     actor :"  Kavin", 
     music :" kadhal assai",
     director : "ravichaandar",
     producer : "rk"
};
console.log( Object.keys(movie));

console.log(Object.values(movie));
